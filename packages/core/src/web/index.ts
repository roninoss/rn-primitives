export { Image, Pressable, Text, View } from './components';
export { Platform } from './platform';

export type {
  DivProps,
  Element,
  ElementTag,
  ImageProps,
  PressableProps,
  TextProps,
  ViewProps,
} from './types';
